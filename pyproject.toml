[project]
name = "PyBash"
version = "0.1.0"
description = "Simple Bash-like Python shell"
requires-python = ">=3.12"
dependencies = [
    "pre-commit>=4.3.0",
    "pyfakefs>=5.10.2",
    "pytest>=8.4.2",
    "pytest-mock>=3.15.1",
    "typer>=0.20.0",
]
